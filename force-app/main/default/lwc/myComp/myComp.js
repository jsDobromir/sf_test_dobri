import { LightningElement } from "lwc";

export default class MyComp extends LightningElement {
    connectedCallback() {
var a = 'not used var'
    var b = "bad format";
    }

badFormattedMethod() {
return "bad format";
    }
}
